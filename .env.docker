# Docker Production Environment Configuration
# This file contains non-sensitive configuration values
# Actual secrets (RUNPOD_API_KEY, etc.) should be in .env (gitignored)

# Application URL (production domain)
NEXT_PUBLIC_APP_URL=https://video.labfab.ca

# Request timeout (milliseconds)
NEXT_PUBLIC_MAX_TIMEOUT=2500

# Node environment
NODE_ENV=production

# Optional: Email for Let's Encrypt SSL certificates
ACME_EMAIL=admin@labfab.ca
